if (!this.bindings) {
    // Perform actions when bindings does not exist
    console.log("Bindings do not exist.");
} else {
    // Perform actions when bindings does exist
    console.log("Bindings exist.");
}
